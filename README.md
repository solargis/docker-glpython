# Python docker with libGL installed

This is just python from [docker](https://hub.docker.com/_/python) enriched by libGL.so (libgl-dev ubuntu package) to allow some headless tests of _PySIde2.QtWidgets_ .
